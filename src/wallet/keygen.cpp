#include <iostream.h>

uint64_t getPublicKeyFromPrivateKey(uint64_t privateKey) {
  uint64_t publicKey = (unit64_t*)malloc(2048) // 2048 bit key for added security (1024 bit keys where cracked late 2018)
  //Main code WILL go here.
  return publicKey;
  }
